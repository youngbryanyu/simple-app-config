/**
 * Enums for data types used in converting environment variables.
 */
export enum DataTypes {
  String = 'string',
  Number = 'number',
  Boolean = 'boolean',
  Object = 'object',
  Date = 'Date',
  Array = 'Array',
  Map = 'Map',
  Set = 'Set',
  RegExp = 'RegExp'
}
